based on the the information of `geph-client help` and a few attempts, the new usage is 

```bash 
sudo ~/geph/geph4-client connect --vpn-mode tun-route auth-password --username "XXXXXX" --password "XXXXXXX"
```

